import styled from 'styled-components'; 

const TagReactLab = styled.h2`
    border.radius: 4px; 
    font-size: 30rem; 
    background-color: var(--color-frontend); 
    padding: 20rem 18rem; 
    color: var(--color-gray-light);
    display: inline-block; 
`;

export default TagReactLab; 
