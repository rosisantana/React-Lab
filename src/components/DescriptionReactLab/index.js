import styled from 'styled-components'; 

const DescriptionReactLab = styled.p`
    font-size: 18rem; 
    color: var(--color-gray-light); 
    font-weight: 300;
`; 

export default DescriptionReactLab; 